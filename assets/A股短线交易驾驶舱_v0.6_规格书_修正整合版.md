# 《A股短线交易驾驶舱 v0.6 程序开发规格书》

# **第一章 开发定位**

## **1.1 软件名称**

《A股短线交易驾驶舱 v0.6》

---

## **1.2 软件定位**

本系统定位：

> 一个基于A股沪深主板短线交易规则的AI辅助决策驾驶舱。
> 

---

## **1.3 非目标**

本系统不是：

❌ 自动交易软件

❌ 股票预测机器人

❌ 自动荐股工具

❌ 涨停预测系统

❌ 量化高频交易系统

---

# **第二章 系统总体架构**

系统采用：

五层结构。

---

# **第一层：数据采集层**

负责：

获取市场原始数据。

包括：

## **市场行情数据**

字段：

**plain**

```
股票代码

股票名称

最新价格

涨跌幅

成交额

成交量

换手率

涨停状态

跌停状态
```

---

## **市场环境数据**

字段：

**plain**

```
上涨数量

下跌数量

涨停数量

跌停数量

炸板数量

炸板率

连板高度

成交金额
```

---

## **板块数据**

字段：

**plain**

```
板块名称

板块涨幅

板块成交额

板块涨停数量

资金流向
```

---

## **个股交易数据**

字段：

**plain**

```
持仓数量

成本价格

当前价格

买入时间

可卖数量

冻结资金
```

---

# **第二层：规则计算层**

负责：

按照v0.6规则计算。

---

# **2.1 市场环境计算模块**

输入：

市场数据。

输出：

**plain**

```
市场环境评分

市场状态
```

例如：

**plain**

```
评分：

76

状态：

震荡
```

---

# **2.2 情绪计算模块**

输入：

市场数据。

输出：

**plain**

```
情绪周期

情绪灯号
```

注意：

程序生成：

"建议值"。

不是最终值。

例如：

**plain**

```
系统分析：

启动阶段

建议灯号：

绿色

人工确认：

待确认
```

---

# **2.3 状态调和模块（v0.6 新增）**

实现交易规则v0.6第3.3.1节保守原则。

**市场环境 × 情绪灯号 → 最终交易等级**

**表格**

| **情绪灯号 \ 市场环境** | **A级（进攻）** | **B级（震荡）** | **C级（防守）** |
| --- | --- | --- | --- |
| 绿灯 | L3 积极 | L2 谨慎 | L1 防守 |
| 黄灯 | L2 谨慎 | L2 谨慎 | L1 防守 |
| 橙灯 | L1 防守 | L1 防守 | L0 停止 |
| 红灯 | L0 停止 | L0 停止 | L0 停止 |

**取值原则：取市场环境等级与情绪灯号等级中较低（更保守）的一档。**

**各等级对应的具体执行参数：**

**表格**

| **最终等级** | **单笔风险阈值** | **新开仓权限** | **仓位系数** | **可执行的交易模式** |
| --- | --- | --- | --- | --- |
| L3 积极 | 0.8%–1.0% | 允许 | 100% | 强势跟随 / 分歧低吸 / 弱转强 |
| L2 谨慎 | 0.5%–0.8% | 允许，仅限核心池 | 70% | 分歧低吸 / 弱转强（不追高） |
| L1 防守 | ≤0.3% | 原则上禁止，需人工在系统内填写理由后特批 | 40% | 仅HOLD / REDUCE，不生成新BUY候选 |
| L0 停止 | 0% | 禁止（系统硬拦截，无特批入口） | 0% | 仅EXIT / REDUCE |

程序实现：

**plain**

```
最终等级 = min(市场环境等级映射值, 情绪灯号等级映射值)
```

首页顶部同时展示两个原始状态和调和后的最终等级。

---

# **2.4 主线评分模块**

输入：

板块数据。

输出：

**plain**

```
主线名称

主线评分

评分明细
```

数据结构（v0.6 六项拆分版）：

**JSON**

```json
{
  "资金强度": 22,
  "板块扩散": 18,
  "政策逻辑": 12,
  "持续性": 8,
  "市场认可": 13,
  "龙头表现": 14,
  "总分": 87
}
```

---

# **2.5 龙头评分模块**

输入：

股票数据。

输出：

**plain**

```
股票名称

龙头评分

评分详情
```

例如：

**JSON**

```json
{
  "辨识度": 18,
  "资金强度": 23,
  "带动能力": 14,
  "技术形态": 13,
  "情绪匹配": 14,
  "风险收益": 8,
  "总分": 90
}
```

---

**计算顺序说明（v0.6 修正补充）**

主线评分与龙头评分存在数据依赖，开发时须按以下顺序计算：

1. **先计算主线评分**：其中「龙头表现」字段使用板块内涨幅最大股票的实时数据
2. **再计算龙头评分**：引用主线归属信息，确保「所属主线」字段已就绪

---

# **第三层：AI分析层**

这是扣子最重要的部分。

必须明确：

AI不是交易员。

AI是分析助手。

---

# **3.1 AI输入内容**

包括：

## **市场摘要**

例如：

**plain**

```
今日涨停数量：

45

昨日涨停反馈：

良好

最高连板：

6板
```

---

## **主线信息**

例如：

**plain**

```
候选方向：

人工智能

评分：

85
```

---

## **个股信息**

例如：

**plain**

```
股票：

XX股份

所属：

人工智能

龙头评分：

88
```

---

# **3.2 AI输出内容**

必须结构化。

不能只输出文字。

格式（v0.6 修正版）：

**JSON**

```json
{
  "判断类型": "辅助分析",
  "市场环境": "震荡",
  "情绪周期": "分歧",
  "情绪灯号": "黄灯",
  "核心逻辑": "资金仍集中",
  "风险提示": "高位股波动增加",
  "建议状态": "观察",
  "数据不足项": "无",
  "是否需要人工确认": "是"
}
```

---

# **3.3 AI禁止输出**

禁止：

"明天必涨"

"可以买入"

"涨停概率90%"

"保证盈利"

---

正确：

应该：

"符合观察条件"

"存在交易机会"

"风险需要关注"

---

# **第四层：风险控制引擎**

这是系统核心。

---

# **4.1 账户风险计算**

数据库字段：

**plain**

```
账户总资金

当前持仓金额

计划买入金额

买入价格

止损价格
```

---

明确定义（v0.6 新增）：

**plain**

```
止损幅度 = |买入价格 - 止损价格| ÷ 买入价格

预计最大亏损 = 持仓金额 × 止损幅度

账户风险比例 = 预计最大亏损 ÷ 账户总资金
```

---

计算：

预计亏损：

**plain**

```
持仓金额 × 止损幅度
```

账户风险：

**plain**

```
预计亏损 ÷ 总资金
```

---

输出：

**plain**

```
风险状态：

正常

账户风险：

0.8%
```

---

**T+1 风险预警（v0.6 修正补充）**

T+1 制度下，当日买入部分次日才能卖出，隔夜存在额外不确定性。系统增加缓冲提示：

**plain**

```
T+1 调整后风险 = 预计最大亏损 × 1.2 ÷ 账户总资金
```

该值仅作 UI 提示，**不触发风险拦截**，用于提醒用户「考虑隔夜风险，实际风险可能高于理论值」。

---

# **4.2 仓位计算**

输入：

账户资金

风险比例

止损幅度

输出：

最大允许买入金额。

公式：

**plain**

```
最大仓位金额 = 账户资金 × 单笔风险 ÷ 止损幅度
```

---

**连续亏损仓位调整（v0.6 修正补充）**

交易规则 v0.5 第 8.9 节两级连续亏损保护：

- **连续 3 笔亏损 → 下一笔仓位上限 = 原计算仓位 × 50%**
- 连续 5 笔亏损 → 暂停 3 个交易日（见 4.3 条件 5）

连续亏损计数与 4.3 条件 5 共享，在人工确认恢复后归零。

程序实现：

**plain**

```
if 连续亏损笔数 >= 3:
    最大仓位金额 = 最大仓位金额 × 0.5
```

---

# **4.3 风险拦截（v0.6 扩充为8条）**

以下情况：

禁止生成BUY候选。

**表格**

| **序号** | **拦截条件** | **触发来源** | **拦截类型** |
| --- | --- | --- | --- |
| 1 | 账户风险超过单笔阈值上限 | 见状态调和模块等级表 | 硬拦截 |
| 2 | 没有止损价格 | 买点四要素检查 | 硬拦截 |
| 3 | T+1风险无法评估（可卖数量/持仓时间数据缺失） | 交易制度校验模块 | 硬拦截 |
| 4 | 关键数据缺失（现价/账户资金/止损价） | 数据管理模块 | 硬拦截 |
| 5 | 连续5笔亏损 → 暂停3个交易日 | 交易记录统计 | 硬拦截，无例外 |
| 6 | 当日累计风险 ≥ 2% | 风险计算模块 | 硬拦截 |
| 7 | 最终交易等级 = L0（红灯，或C级+橙灯组合） | 状态调和模块 | 硬拦截 |
| 8 | 最终交易等级 = L1 且未完成人工特批理由填写 | 状态调和模块 | 硬拦截，特批理由写入is_violation |

以上8条中，1/2/3/4/6/7为系统硬拦截（不生成BUY候选，无UI层面的例外入口）；5为硬拦截无例外；8允许人工在明确记录理由后继续，理由文本写入交易记录表`is_violation`字段，用于后续复盘统计"违规交易"胜率，倒逼纪律。

---

# **第五层：人工决策层**

这是必须保留的。

---

# **5.1 人工确认按钮**

所有交易建议：

必须经过：

## **确认交易**

或者：

## **放弃交易**

---

# **5.2 人工修改权限**

允许修改：

- 情绪周期
- 情绪灯号
- 龙头身份
- 主线判断

但必须记录：

修改原因。

---

# **第六章 数据库设计**

---

# **6.1 股票表 stock**

字段：

**plain**

```
stock_id

code

name

market

industry

status  -- 增加过滤枚举：normal / excluded_st / excluded_delisting / excluded_suspended / excluded_non_mainboard / excluded_liquidity
```

---

# **6.2 市场环境表 market_status**

字段：

**plain**

```
date

score

environment

emotion_cycle

emotion_light

final_level  -- v0.6 新增：最终交易等级 L0-L3

confidence
```

---

# **6.3 主线表 theme**

字段：

**plain**

```
date

theme_name

score

capital_score

spread_score

policy_score

expectation_score

recognition_score

leader_performance_score  -- v0.6 新增：龙头表现 15分
```

---

# **6.4 股票评分表 stock_score**

字段：

**plain**

```
date

stock_code

theme

leader_score

recognition

capital

drive

technical

emotion

risk_reward
```

---

# **6.5 交易记录表 trade（v0.6 扩充版）**

字段：

**plain**

```
trade_id

stock_code

buy_time

sell_time

buy_price

sell_price

position

stop_price

profit_loss

status  -- 枚举：open / closed / paused

-- v0.6 新增字段：

trade_mode          VARCHAR   -- 交易模式：强势跟随/分歧低吸/弱转强

theme               VARCHAR   -- 所属主线

market_env          VARCHAR   -- 买入时市场环境（A/B/C）

emotion_cycle       VARCHAR   -- 买入时情绪周期（六阶段）

emotion_light       VARCHAR   -- 买入时情绪灯号

final_level         VARCHAR   -- 买入时最终交易等级（L0-L3）

buy_reason          TEXT      -- 买入逻辑（对应买点四要素-1）

buy_position_note   TEXT      -- 买入位置说明（对应买点四要素-2）

risk_note           TEXT      -- 风险位置/止损理由（对应买点四要素-3）

exit_plan           TEXT      -- 退出计划（对应买点四要素-4）

sell_reason         TEXT      -- 卖出理由

avg_cost            DECIMAL   -- 平均成本（多次加仓后自动计算）

add_records         JSON      -- 加仓记录 [{time, price, qty}]

reduce_records      JSON      -- 减仓记录 [{time, price, qty, reason}]

is_violation        BOOLEAN   -- 是否违反系统建议开仓（对应拦截条件5/8的人工特批）

violation_reason    TEXT      -- 违规理由（特批时必填）

decision_score      INT       -- 决策质量评分（0-100）

stock_role          VARCHAR   -- 龙头/核心/跟风
```

一笔交易（trade_id）在加仓、减仓、分批卖出过程中始终保持同一ID，`add_records`/`reduce_records`记录完整轨迹，`avg_cost`随加仓自动重算，直到`可卖数量=0`时该trade状态转为`closed`。

---

# **第七章 首页页面开发规范**

首页：

必须包含：

---

## **模块1**

账户风险

---

## **模块2**

市场环境

---

## **模块3**

情绪状态

---

## **模块4**

当前主线

---

## **模块5**

核心股票池

---

## **模块6**

交易建议状态

---

页面顶部固定显示：

当前系统状态：

六选一：

**plain**

```
观察 / BUY候选 / HOLD / REDUCE / EXIT / NO DATA
```

---

**UI 区分规范（v0.6 修正补充）**

| **状态** | **UI 表现形式** |
| --- | --- |
| 观察 | 灰色信息提示条 |
| BUY候选 | 绿色 actionable 提示条 |
| HOLD | 蓝色持有提示条 |
| REDUCE | 橙色减仓提示条 |
| EXIT | 红色退出提示条 |
| NO DATA | 红色警示条 + 缺失字段清单 |

---

**首页六大模块的具体展示逻辑、优先级顺序、字段说明，以交易规则v0.5第十三章（已同步为v0.6）为准，本规格书不重复定义。**

---

# **第八章 AI提示词模板**

扣子AI模块建议固定Prompt。

---

系统角色：

> 你是一名A股短线交易辅助分析AI，不进行自动交易，不预测未来涨跌，只根据提供的数据和交易规则进行结构化分析。
> 

---

输入：

**plain**

```
市场数据：

xxx

情绪状态：

xxx

主线：

xxx

股票：

xxx
```

---

输出：

必须：

1. 当前状态
2. 核心逻辑
3. 风险因素
4. 数据不足项
5. 是否需要人工确认

---

# **第九章 开发顺序建议**

不要一次开发全部。

建议：

## **第一阶段：**

交易规则引擎

开发：

- 股票池
- 市场环境
- 风险计算
- 交易记录

---

## **第二阶段：**

AI分析

开发：

- 主线分析
- 龙头分析
- 复盘

---

## **第三阶段：**

手机驾驶舱

开发：

- 首页
- 股票池
- 交易管理

---

## **第四阶段：**

优化

增加：

- 历史回测
- 统计分析
- 交易复盘

---

# **第十章 最终开发验收标准**

系统必须满足：

---

## **规则一致性**

v0.6规则全部可追溯。

---

## **风险优先**

任何交易建议：

先经过风险检查。

---

## **AI可解释**

AI必须说明：

为什么。

---

## **不自动交易**

保留人工最终权限。

---

## **数据不足不乱判断**

支持：

NO DATA。

---

# **附录A：数据管理模块（v0.6 新增）**

实现交易规则v0.5第十一章数据管理要求。

## **A.1 数据三属性**

每条数据字段附带：

- `source`：行情接口 / AI分析 / 人工输入
- `fetch_time`：获取时间
- `valid_until`：有效期截止时间

## **A.2 数据状态四分类**

- `valid`（有效）：来源正常、时间有效、无明显异常，允许参与系统计算
- `expired`（过期）：超过有效期但可参考，必须标记"过期"
- `missing`（缺失）：部分数据缺失，降低系统可信度
- `invalid`（无效）：行情异常等，禁止进入决策

## **A.3 数据可信度评分**

**plain**

```
数据可信度 = 行情完整度×0.4 + 更新时效性×0.3 + 来源可靠性×0.3 （100分制）
```

低于阈值（70分）自动进入"谨慎模式"：UI提示，不禁止查看但禁止生成BUY候选。

## **A.4 数据更新频率**

- 高频数据（盘中更新）：股价、成交量、涨跌幅、涨停状态
- 中频数据（每日更新）：市场环境评分、主线评分、龙头评分
- 低频数据（阶段更新）：政策逻辑、产业趋势、主线持续性

## **A.5 人工输入标记**

第一阶段开发允许人工输入，但必须标记数据来源：

**plain**

```
涨停数量：

数据来源：人工输入

更新时间：09:25
```

系统不能把人工输入当作自动数据。

---

# **附录B：股票过滤模块（v0.6 新增）**

在数据采集层增加过滤器，股票进入池前自动排除：

**plain**

```
非沪深主板（创业板/科创板/北交所）
ST / *ST
退市整理期
停牌
流动性异常（近5日日均成交额低于5000万，可配置）
```

被排除股票禁止进入核心池/观察池，即使涨停也不参与主线/龙头评分计算。

---

# **附录C：交易制度校验模块（v0.6 新增）**

开仓/卖出前依次校验：

**plain**

```
1. 交易日判断（非节假日、非周末）

2. 交易时间判断：
   9:15–9:25  集合竞价（只观察，不生成BUY候选）
   9:30–11:30 / 13:00–15:00  盘中（唯一允许生成BUY候选的时段）
   14:57–15:00  收盘集合竞价（仅监控，不新开仓）

3. 停牌状态检查

4. 涨跌停状态检查（涨停不可买/跌停不可卖，需实时判断"一字板"无法成交的情形）

5. 最小交易单位检查（100股整数倍，卖出允许零股）

6. T+1可卖数量检查（当日买入部分不计入可卖数量）

7. 交易费用估算（佣金+印花税(卖出)+过户费，计入实际盈亏，但不计入实时风险拦截）

8. 除权除息调整（持仓成本价自动重算，避免除权日误判止损触发）
```

---

# **最终系统形态**

完成后：

它不是：

"AI帮你炒股"。

而是：

> 一个把短线交易高手的思考流程，结构化、数据化、辅助化的AI交易驾驶舱。
>