# DESIGN.md

## 项目画像
A股短线交易AI辅助决策驾驶舱。移动端优先，PC端增强。核心用户是短线交易者，在盘前/盘中/收盘三个时段高频使用。信息密度高，决策压力大，需要"一眼看清状态，两秒判断风险"。

## 气质与意象
专业交易终端。暗色背景降低屏幕眩光，数据如机场塔台仪表般清晰排列。红绿色严格遵循A股涨跌惯例（红涨绿跌），但状态等级灯号使用交通灯色系（绿=安全通行，黄=注意，橙=警告，红=停止）——两套颜色体系各司其职，不混淆。

## Design Tokens

### 色彩
- 背景层：深炭灰 `#0a0e17`（主背景）/ `#131825`（卡片）/ `#1c2333`（悬浮/输入框）
- 文字主：`#e8ecf4`；次要：`#8b95a8`；禁用：`#4a5568`
- 涨（A股惯例）：`#ef4444` 红；跌：`#22c55e` 绿
- 状态灯号：绿灯 `#22c55e` / 黄灯 `#eab308` / 橙灯 `#f97316` / 红灯 `#ef4444`
- 等级 L0：`#ef4444`（停止）/ L1：`#f97316`（防守）/ L2：`#eab308`（谨慎）/ L3：`#22c55e`（积极）
- 品牌强调：`#3b82f6`（蓝，仅用于交互元素和可点击项，不用于涨跌）
- 边框：`#2a3344`

### 字体
- 数字：`JetBrains Mono`, `SF Mono`, monospace（所有价格、百分比、金额）
- 中文：`"PingFang SC"`, `"Microsoft YaHei"`, system-ui, sans-serif
- 标题字重 600，正文字重 400，数字字重 500

### 间距与圆角
- 间距基数 4px，卡片内边距 16px（移动端紧凑 12px）
- 圆角：卡片 8px，按钮 6px，输入框 6px，徽章 4px

### 阴影
- 暗色模式下阴影用边框代替，卡片边框 `1px solid #2a3344`
- 危险状态（L0/EXIT）用左侧 3px 红色色条，不用发光阴影（避免刺眼）

## 布局与响应式
- 移动端（<768px）：单列布局，顶部固定系统状态条，六大模块纵向堆叠
- PC端（≥768px）：12栅格，账户风险和市场环境并排，股票池表格化
- 关键数字使用大字号（24-32px），辅助信息小字号（12px）

## 组件规范
- 状态提示条：全宽，高度40px，左侧3px色条 + 状态文字 + 右侧操作按钮
- 数据卡片：标题行（标签+来源图标+更新时间）+ 主数据区 + 底部说明
- 风险数字：账户风险比例使用颜色编码（<阈值绿，接近阈值橙，超阈值红）
- 按钮：主操作蓝色实心，次操作描边，危险操作红色描边，禁用态灰色

## 交互与状态
- 数据加载：骨架屏（skeleton），不用 spinner
- 数据过期：卡片右上角橙色"已过期"角标，数据仍可查看但置灰
- NO DATA：红色警示条 + 缺失字段清单，不显示0或空白
- 人工确认操作：弹窗二次确认，显示修改前后对比
- 风险拦截：拦截原因必须具体（"账户风险1.2%超过L2阈值0.8%"而非"风险过高"）

## 设计禁忌
- 禁止使用紫蓝渐变作为主色调（AI套话审美）
- 禁止用发光/霓虹效果装饰（交易场景需要冷静克制）
- 禁止用绿色表示"涨"（A股惯例是红涨绿跌，灯号绿色表示安全，两者不可混淆）
- 禁止数据为0时显示空白（必须明确显示"0"或"--"）
- 禁止在移动端使用hover状态作为唯一交互方式
